{
  "scripts": {
    "start": "concurrently \"npm run tsc\" \"npm run serve\"",
    "serve": "wds --watch --open --app-index=public/index.html",
    "tsc": "tsc -w",
    "minify": "minify dist/HorizontalScroll.js > dist/HorizontalScroll.min.js"
  },
  "devDependencies": {
    "@web/dev-server" : "^0.1.2",
    "concurrently" : "^5.3.0",
    "less": "^4.1.0",
    "minify" : "^7.0.0",
    "typescript" : "^4.1.3"
  }
}
